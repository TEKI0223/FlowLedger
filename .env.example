# 本地开发：默认 file:./data/flowledger.db，可以不设。
# 生产（Vercel + Turso）：把下面两个填进 Vercel 项目 Settings → Environment Variables。
DATABASE_URL=file:./data/flowledger.db
# DATABASE_URL=libsql://<your-db-name>-<your-org>.turso.io
# DATABASE_AUTH_TOKEN=<turso db tokens create 输出>

# 访问密码：生产环境设置后整站需要登录，本地一般不设。
# 改密码会立即作废所有现存 session，因为 session JWT 由密码派生 secret 签名。
# FLOWLEDGER_PASSWORD=
